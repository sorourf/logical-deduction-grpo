# torch is provided by the RunPod PyTorch template — don't pin or reinstall.
# Locally, install with: pip install torch
transformers==4.51.3
peft==0.15.2
datasets>=2.19.0
jinja2>=3.1
accelerate>=0.30
rich==14.0.0
numpy
pandas